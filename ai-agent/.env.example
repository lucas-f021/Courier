# Anthropic
ANTHROPIC_API_KEY=

# Slack
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=
SLACK_APP_TOKEN=

# OpenAI (only needed if USE_OPENAI_MODEL = True in main.py)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Ollama (only needed if USE_LOCAL_MODEL = True in main.py)
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen3:8b

# Note: Gmail does not use .env variables.
# credentials.json must be downloaded from Google Cloud Console.
# token.json is auto-generated on first run via OAuth.
